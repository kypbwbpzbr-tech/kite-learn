# 风筝学堂 - 腾讯云部署指南

## 一、创建腾讯云服务器

### 1. 登录腾讯云控制台
访问 https://console.cloud.tencent.com/

### 2. 创建轻量应用服务器
1. 点击「云产品」→「轻量应用服务器」
2. 点击「新建」
3. 选择配置：
   - **地域**：广州（或其他离你近的）
   - **可用区**：随便选
   - **镜像**：Ubuntu 22.04 LTS
   - **套餐**：2核2G（约¥100/月）或 2核4G（约¥200/月）
   - **购买时长**：1个月（先试试）
4. 设置密码：记住你设置的密码
5. 点击「立即购买」

### 3. 获取服务器 IP
创建完成后，在服务器列表里找到你的服务器，记下公网 IP（例如：123.123.123.123）

---

## 二、连接服务器

### Windows 用户
1. 下载并安装 PuTTY 或使用 Windows Terminal
2. 连接命令：
   ```bash
   ssh root@你的服务器IP
   ```
3. 输入你设置的密码

### Mac 用户
打开终端，输入：
```bash
ssh root@你的服务器IP
```

---

## 三、安装 Docker

连接到服务器后，依次执行以下命令：

```bash
# 更新系统
apt update && apt upgrade -y

# 安装 Docker
curl -fsSL https://get.docker.com | sh

# 启动 Docker
systemctl start docker
systemctl enable docker

# 验证安装
docker --version
```

---

## 四、部署风筝学堂

### 方法 1：使用 Git（推荐）

```bash
# 安装 Git
apt install git -y

# 克隆项目
git clone https://github.com/你的用户名/kite-learn.git
cd kite-learn

# 配置环境变量
cp .env.local .env.production
nano .env.production
```

修改 `.env.production` 中的环境变量（如果需要），保存退出（Ctrl+X → Y → Enter）

```bash
# 启动服务
docker compose up -d

# 查看状态
docker compose ps
```

### 方法 2：上传文件

1. 在本地用 SCP 上传项目：
   ```bash
   scp -r ./kite-learn root@你的服务器IP:/root/
   ```

2. 在服务器上：
   ```bash
   cd /root/kite-learn
   docker compose up -d
   ```

---

## 五、访问测试

打开浏览器，访问：
```
http://你的服务器IP:3000
```

如果看到风筝学堂页面，说明部署成功！

---

## 六、常用命令

```bash
# 查看日志
docker compose logs -f

# 停止服务
docker compose down

# 重启服务
docker compose restart

# 更新代码后重新部署
git pull
docker compose up -d --build
```

---

## 七、配置域名（可选）

如果需要域名访问：

1. 在域名服务商添加 A 记录，指向你的服务器 IP
2. 安装 Nginx：
   ```bash
   apt install nginx -y
   ```
3. 配置 Nginx：
   ```bash
   nano /etc/nginx/sites-available/kite-learn
   ```

   添加以下内容：
   ```nginx
   server {
       listen 80;
       server_name 你的域名.com;

       location / {
           proxy_pass http://localhost:3000;
           proxy_http_version 1.1;
           proxy_set_header Upgrade $http_upgrade;
           proxy_set_header Connection 'upgrade';
           proxy_set_header Host $host;
           proxy_cache_bypass $http_upgrade;
       }
   }
   ```

4. 启用配置：
   ```bash
   ln -s /etc/nginx/sites-available/kite-learn /etc/nginx/sites-enabled/
   nginx -t
   systemctl restart nginx
   ```

---

## 注意事项

1. **环境变量**：确保 `.env.production` 中的 Supabase 配置正确
2. **防火墙**：腾讯云默认开放 80 和 443 端口，3000 端口可能需要在控制台开放
3. **HTTPS**：如果需要 HTTPS，可以使用 Certbot 免费证书
4. **备份**：定期备份数据库和代码
